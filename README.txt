There is no need to provide any additional default values or terminal commands because this project is implemented on Google Colab. This.ipynb files can be opened with Google Colab or Jupyter Notebook by simply running it. One important thing is that in order to correctly read the required data files, the user must have the files added to the project folder. This is because the required data files are read straight from the project structure.
